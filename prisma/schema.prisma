generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = "mongodb+srv://muzamil:BsEagDQ9bZOBq5Vj@cluster0.91h9ks2.mongodb.net/first_registration?retryWrites=true&w=majority&appName=AtlasApp"
}

model User {
  id                            String   @id @default(auto()) @map("_id") @db.ObjectId
  email                         String
  fullname                      String
  dateOfBirth                   DateTime // Changed to DateTime for Date Of Birth
  gender                        String
  maritalStatus                 String
  nationality                   String
  passportNumber                String
  expiryOfPassport              DateTime // Changed to DateTime for Expiry of Passport
  proofOfPakistaniOrigin        String
  idNumberForProofOfOrigin      String
  occupation                    String
  detailsOfOccupation           String
  visaStatus                    String
  expiryOfPermitVisa            DateTime // Changed to DateTime for Expiry of Permit/Visa
  stayingInRawandaSince         DateTime // Changed to DateTime for Staying in Rwanda Since
  livingWithDependantsInRawanda Boolean
}
